name: Mobile M6-D1 AFTER

on:
  push:
    branches:
      - "audit/mobile-m6-d1-after-20260825"
    paths:
      - ".github/workflows/mobile-m6-d1-after.yml"
      - ".audit/mobile-m6-d1-after.html"
      - ".audit/mobile-m6-d1-after.tsx"
      - ".audit/mobile-m6-d1-after.mjs"

permissions:
  contents: read

jobs:
  after:
    name: Exact-head notifications certification
    runs-on: ubuntu-latest
    env:
      PRODUCT_HEAD: cdac655b20b54e3a3cb7262fd1b5a634c9a30ede
    steps:
      - name: Checkout audit harness
        uses: actions/checkout@v4
        with:
          path: audit
      - name: Checkout exact product HEAD
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PRODUCT_HEAD }}
          path: product
      - name: Assert exact product HEAD
        run: test "$(git -C product rev-parse HEAD)" = "$PRODUCT_HEAD"
      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: product/frontend/package-lock.json
      - name: Install frontend and Chromium
        working-directory: product/frontend
        run: |
          npm ci
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Materialize AFTER harness outside product commit
        run: |
          cp audit/.audit/mobile-m6-d1-after.html product/frontend/mobile-m6-d1-after.html
          cp audit/.audit/mobile-m6-d1-after.tsx product/frontend/src/mobile-m6-d1-after.tsx
          cp audit/.audit/mobile-m6-d1-after.mjs product/frontend/mobile-m6-d1-after.mjs
      - name: Capture and exercise M6-D1 AFTER
        working-directory: product/frontend
        run: node mobile-m6-d1-after.mjs
      - name: Upload AFTER evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-m6-d1-after
          path: product/frontend/mobile-m6-d1-after-artifacts
          retention-days: 14
