name: Mobile M6-G1 AFTER

on:
  push:
    branches:
      - "audit/mobile-m6-g1-before-20260825"
    paths:
      - ".github/workflows/mobile-m6-g1-after.yml"

permissions:
  contents: read

jobs:
  after:
    name: M6-G1 exact-head visual AFTER
    runs-on: ubuntu-latest
    env:
      PRODUCT_HEAD: feb8bfc33b6385ed14dae9d1d5e56cb5edd53ae4
    steps:
      - name: Checkout audit evidence
        uses: actions/checkout@v4
        with:
          path: audit

      - name: Checkout exact product HEAD
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PRODUCT_HEAD }}
          path: product

      - name: Assert exact product HEAD
        run: test "$(git -C product rev-parse HEAD)" = "$PRODUCT_HEAD"

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: product/frontend/package-lock.json

      - name: Install frontend and Chromium
        working-directory: product/frontend
        run: |
          npm ci
          npm install --no-save --package-lock=false --ignore-scripts playwright@1.62.1
          npx playwright install --with-deps chromium

      - name: Materialize versioned AFTER harness
        run: |
          cp audit/.audit/mobile-m6-g1-after.html product/frontend/mobile-m6-g1-after.html
          cp audit/.audit/mobile-m6-g1-after.tsx product/frontend/src/mobile-m6-g1-after.tsx
          cp audit/.audit/mobile-m6-g1-after.mjs product/frontend/mobile-m6-g1-after.mjs

      - name: Capture and assert M6-G1 AFTER
        working-directory: product/frontend
        run: PRODUCT_HEAD="$PRODUCT_HEAD" node mobile-m6-g1-after.mjs

      - name: Upload AFTER artifact
        uses: actions/upload-artifact@v4
        with:
          name: mobile-m6-g1-after
          path: product/frontend/mobile-m6-g1-after-artifacts/
          if-no-files-found: error
          retention-days: 14
