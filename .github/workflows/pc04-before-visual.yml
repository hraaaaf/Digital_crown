name: PC-04 BEFORE Visual Evidence

on:
  pull_request:
    branches: [master]
    paths:
      - .github/workflows/pc04-before-visual.yml
      - frontend/scripts/capture-pc04-before.mjs
      - frontend/src/features/patient-companion/**
      - docs/clinic/patient_companion/**

concurrency:
  group: pc04-before-${{ github.ref }}
  cancel-in-progress: true

jobs:
  before:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Materialize true BEFORE
        shell: bash
        run: |
          set -euo pipefail
          git worktree add /tmp/pc04-before "${{ github.event.pull_request.base.sha }}"
          cd /tmp/pc04-before/frontend
          npm ci
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium webkit
          npm run dev -- --host 127.0.0.1 --port 5178 >/tmp/pc04-before.log 2>&1 &
          for i in {1..60}; do curl -fsS http://127.0.0.1:5178/ >/dev/null && break || sleep 1; done
      - name: Capture BEFORE
        working-directory: frontend
        env:
          PC04_BEFORE_URL: http://127.0.0.1:5178
          PC04_EVIDENCE_DIR: ../artifacts/pc04-consent-before
        run: |
          npm ci
          npm install --no-save --package-lock=false playwright@1.55.0
          node scripts/capture-pc04-before.mjs
      - name: Assert evidence
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          items=json.loads(Path("artifacts/pc04-consent-before/evidence.json").read_text())
          assert len(items)==4
          assert all(not x["horizontalOverflow"] for x in items)
          print("pc04_before=PASS")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: pc04-consent-before
          path: artifacts/pc04-consent-before
          if-no-files-found: error
          retention-days: 30
