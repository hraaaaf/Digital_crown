name: Patient P0-E Payment After

on:
  push:
    branches:
      - agent/patient-p0e2-payment-ui-truth
    paths:
      - .github/workflows/patient-p0e-payment-after.yml
      - frontend/scripts/capture-patient-p0e-payment-after.mjs
      - frontend/src/features/patients/components/QuickPayModal.tsx
      - frontend/src/features/patients/components/PayActeModal.tsx
      - frontend/src/features/patients/components/PaymentMethodExplicit.p0e.test.tsx
      - frontend/src/features/patients/components/PatientFinances.tsx
      - scripts/t2_runtime_server.py
  workflow_dispatch:

concurrency:
  group: patient-p0e-payment-after-${{ github.ref }}
  cancel-in-progress: true

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      ENVIRONMENT: test
      SECRET_KEY: patient-p0e-payment-after-secret-000001
      DATABASE_URL: sqlite:///./t2-runtime-cert.db
      TELEMETRY_ENABLED: "false"
      CLOUD_AI_ENABLED: "false"
      DEBUG: "false"
      ALLOWED_ORIGINS: "http://127.0.0.1:5173,http://localhost:5173"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cpu
          grep -vE '^(torch|torchvision)([<=>].*)?$' requirements.txt > /tmp/requirements-p0e-payment-after.txt
          pip install -r /tmp/requirements-p0e-payment-after.txt
          pip install httpx==0.27.2
      - name: Install frontend and Chromium
        working-directory: frontend
        run: |
          npm ci
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run focused frontend contract
        working-directory: frontend
        run: npx vitest run src/features/patients/components/PaymentMethodExplicit.p0e.test.tsx
      - name: Build frontend
        working-directory: frontend
        run: npm run build
      - name: Start isolated backend
        run: |
          python scripts/t2_runtime_server.py > /tmp/p0e-payment-after-backend.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:8005/health >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/p0e-payment-after-backend.log
          exit 1
      - name: Start frontend
        working-directory: frontend
        run: |
          npm run dev -- --host 127.0.0.1 --port 5173 > /tmp/p0e-payment-after-frontend.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:5173 >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/p0e-payment-after-frontend.log
          exit 1
      - name: Capture payment after
        working-directory: frontend
        run: |
          set -euo pipefail
          rm -rf ../artifacts/patient-p0e-payment-after
          mkdir -p ../artifacts/patient-p0e-payment-after
          node scripts/capture-patient-p0e-payment-after.mjs 2>&1 | tee ../artifacts/patient-p0e-payment-after/runner.log
          test -f ../artifacts/patient-p0e-payment-after/summary.json
          test -f ../artifacts/patient-p0e-payment-after/evidence.json
          test "$(find ../artifacts/patient-p0e-payment-after -maxdepth 1 -name '*.png' | wc -l)" -eq 8
      - name: Upload after evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: patient-p0e-payment-after
          path: artifacts/patient-p0e-payment-after
          if-no-files-found: error
          retention-days: 14
