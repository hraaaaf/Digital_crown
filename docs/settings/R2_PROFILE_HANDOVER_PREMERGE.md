# R2 Profile — Pre-merge handover checkpoint

Branch: `settings-r2-profile`
State: implementation present, certification pending.

Exact next sequence:
1. open PR to `master`;
2. inspect CI and R2 visual workflow once;
3. fix any failure;
4. inspect downloaded AFTER captures;
5. score visual result;
6. update `SETTINGS_PRODUCT_COMPASS.md` with exact proof;
7. merge only after green proof.

No Vercel deployment.
