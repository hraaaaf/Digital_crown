# Canonical dependency source for Digital Crown runtime/build.
# Keep this file as an alias so historical tooling using `-r requirements.txt`
# resolves the exact cabinet lock instead of maintaining a second dependency set.
-r backend/requirements.txt
